# Consumed P346 filter snapshot

`readonly_child.inc.c` is the exact retained public C fragment from the consumed
P346 candidate build's `inputs/p346-readonly-child.inc.c`, copied without edits.
It is 21,792 bytes, SHA-256
`a053e6796a83767e5de97ed714080a10888e2c9a9ef99dbb06e3aabf1633ae29`.

This historical fixture reproduces the `clock_nanosleep` denial independently
of future production-source changes. It is test input, not a candidate source,
activation, approval, or device replay path. It contains no private payload or
raw device evidence.
