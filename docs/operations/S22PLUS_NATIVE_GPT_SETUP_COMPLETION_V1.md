# Exact G2 Android setup-read completion

Status: **REVIEW_GATED_CAPABILITY**; not a new grant or standing recovery lane.

This separate fixed entry specializes only the unconditional stopped gate for
the completed missing-su read during P397 G2 Android initialization. The original
G2 scope already authorizes operator Magisk setup, initial rooted GPT/statfs
health, one ordinary Android reboot and final health. No uncertain mutable
effect is retried. This creates no partition, reset, transfer, deferred or
extra-reboot authority and does not alter the original ten-step sequence.

The immutable private incident binds the original operation/task/grant/review,
all first 20 journal rows, seven preceding raw-rederived results, exact completed
root-read return code 127 with missing-su stderr, and fresh post-setup numeric
root with original A/supporting partition hashes. The original failed read
remains NO_PROOF. Original files, approvals, source hashes and journal rows are
never replaced. The separate source-bound independent PASS_GO names only this
entry, this policy and that exact incident; it grants no action by itself.
The retained properties and fresh rooted health also record whether the boot
changed during operator setup. That observation establishes neither its cause
nor completion of the separately planned verification reboot.

Before any read or reboot, the entry rechecks the exact incident and preceding
immutable result/journal receipts, original global owner/consumption, original host boot, unexpired grant,
and unchanged original 54 host/155 native source bindings. It appends a single
completion admission with the actual operator setup/root-request statement.
Successful machine root proof satisfies setup readiness without requesting a
redundant visual root confirmation. No new operation capacity is consumed.
Full semantic/raw rederivation occurs at admission, the reboot callback and
closure; ordinary command guards reuse those immutable receipts rather than
reparsing all earlier native sessions inside every Android read deadline.

Only fresh `android-initial`, first `android-reboot`, and `android-final` may
follow. The unchanged Android reader provides full same-boot rooted GPT/statfs
brackets. The unchanged reboot adapter calls the new reviewed callback only
after its ordinary launch checks. That callback rederives the initial health,
rechecks the original grant and owner, refuses any existing reboot intent, and
appends the original planned reboot intent before dispatch. It never calls
GPT, reset, Download, N or A transfer machinery.

Once a reboot intent exists, only its original raw completion can be recovered;
it can never dispatch again. A subsequent failure retains the owner and uses
the existing A-health recovery rules. Complete retained results may be repaired
in H0 without device reads, including after result publication cuts. Creating
the completion's own normal records does not invalidate its bound original
journal prefix. Unchanged `Session.close(normal_steps())` still requires every
original result, exact effect ordering and changed-boot GPT/capacity/root proof.
The terminal reports completion of the original plan; the stop and supplemental
entry provenance remain explicit in the same journal and incident report.

The only narrowed gate blocked continuation after a known setup-only read
failure. This specialization ends when this operation closes or its original
grant expires, and must be reviewed if its incident predicate or remaining
effect scope changes. An uncertain reboot, different error, changed image,
earlier stop, new recovery choice or another effect is outside this entry.
An additional setup restart is not included or credited as the verification
reboot. No common partition exception, old approval or consumed claim changes.
