# S22+ P384 native roundtrip followup V2

Status: **REVIEW_GATED_CAPABILITY; no current device grant.**
Target: **SM-S906N / g0q / S906NKSS7FYG8**.

This is a separate policy decision for one new attended qualification after
P383's consumed NO_PROOF transaction and completed exact Android recovery. It
does not renew, rename or release the V1 exception or its global consumed claim.
The [V1 policy](S22PLUS_NATIVE_ROUNDTRIP_FIRST_QUALIFICATION_V1.md), its common
digest and all P383 intents/results/approvals remain unchanged. The user's
follow-up request authorizes this H0 definition and implementation, not a device
effect or a waiver of fresh finite attended Process-v2 approval.

## Exact closed exception

After separate independent common-boundary review, this file is incorporated
by SHA-256 under Common F1 Invariants. Only this exact P384 transaction may use
the following specialization of the permanent content-keyed no-repeat rule:

- Candidate is previously unconsumed **P384 / v0.2.0-rc.2**, run identity
  `c384f1e0a90b5e6d7c8a9b0c1d2e3f0b`, under fixed manifest ID
  `s22plus-fyg8-p384-native-roundtrip-v2-ready-1`.
- Native AP N is 31,150,121 bytes, SHA-256
  `4845ee93971bafcd73042128eeeae676715de9ede4a306d15a21e9db7c5a7d5c`.
  Its sole regular `boot.img.lz4` member is 31,141,009 bytes, SHA-256
  `9ba1097d6a1b6d0f3ab774737909c426d7630c87d0e76bb00f82f737979dc802`.
- Android A is the existing exact Magisk recovery AP: 23,367,721 bytes, SHA-256
  `d2373bf88dda342709440dc3db468f11d80a4593856768a4d8ae402bef215a56`.
  Its exact AP/member paths, sizes and hashes are reopened and bound before
  effects. A and N must be distinct, present, readable and unchanged.
- There are exactly three ordered roles, at most one intent/effect each:
  **N installation, one same-N restoration, one A cleanup/fallback**.
  The installation retains the ordinary permanent target/content consumed
  claim. A distinct immutable global V2 claim is written before installation
  intent at `workspace/private/device-action/s22plus-native-roundtrip-followup-v2.json`.
  V1's claim is never consulted as V2 authority, modified, replaced or removed.
- N restoration is reachable only during normal execution of this declared
  transaction after completed installation, raw-authenticated first native
  health, one sealed CONTROL intent and exact timely Download arrival. It has
  its own exclusive intent and delivery record before first delivery. It never
  releases the installation claim and cannot be ordinary candidate dispatch,
  a recovery action, a retry or a new run with renamed metadata.
- The original research window is **1,800 seconds**, starting once at the V2
  claim. Each native observer has **60 seconds**; each sealed CONTROL retains
  its original **30-second** return window. No stage or changed boot renews a
  deadline, reservation or effect budget. This definition is not a reusable
  session grant and expires for research on its one terminal result or stop.

Failed/missing/uncertain installation or restoration, failed native health,
unexplained session/transport failure, expired observation or research time,
identity drift, or lost physical recovery stops research. At most one CONTROL
is admitted per arrival, with none after a stop. There is no new authentication
within an arrival, repeated N, second delivery attempt or effect inferred from
an intent-only cut. Recovery cannot run restoration or manufacture missing
completion/health. The ordinary content-keyed registry algorithm is unchanged.

After a research stop the only later partition effect is the one predeclared A
fallback, with current exact Download/target binding, unchanged A, continued
physical attendance and demonstrated usable physical Download recovery. Durable
records must exclude any previous A attempt. Missing/uncertain recovery or target
parks. Failed/missing/uncertain A completion parks without replay. Read-only
observation and local finalization can resume from durable evidence; they cannot
authorize another transfer. This is the same narrow boundary-7 recovery shape
as V1, separately granted for V2; it never permits research after failure.

Boot-only membership, all forbidden partitions/transports, root ownership,
private evidence and target isolation remain unchanged. This deliberately adds
one closed permanent-boundary exception; it is not a semantic-only refactor.
Another qualification or standing native-baseline adoption is a separate policy
decision. Neither manifest selection nor H0/PASS_GO grants device authority.

## Native observation and terminal proof

P384 retains [local-display-v1](S22PLUS_FYG8_LOCAL_DISPLAY_LIFECYCLE_V1.md) and the
reviewed [ordinary adoption](S22PLUS_FYG8_LOCAL_DISPLAY_ADOPTION_V1.md) behavior.
Only this explicitly selected V2 graph specializes the ordinary one-N/one-A
scope. Ordinary P384 manifests remain ordinary. Its 108 candidate byte inputs
and AP are unchanged; host/policy changes require fresh execution/static/live
bindings without repointing earlier H0 or consumed records.

Each arrival requires fresh authenticated bootstrap, fixed root/PID1-parentage/
proc-sys-dev mount health, lossless bounded command output/terminal and fresh
idle STATUS. Full P384 raw replay admits the optional HUD EXEC and actual
CONTROL5/6; it does not loosen P383's fixed replay. Optional HUD failure or
omission neither supplies nor invalidates native health. Malformed framing or
authentication still stops the session.

Before second CONTROL, both authenticated kernel boot identity and challenge
must differ from the first arrival. The same N transfer bytes, both health
proofs, both timely exact Download returns, completed A and final rooted FYG8/
original partition health are separately reopened before terminal PASS. CONTROL
ACK is acceptance only. Cleanup cannot promote NO_PROOF to roundtrip success.
Raw native evidence and all role intents/results remain private and append-only;
ordinary journal entries contain bounded state/receipt hashes and the linked
three-role timeline. No extra transfer follows a parser/publication failure.

## Qualification and activation

Independent review covers the common exception, target adoption, profile-based
owner, pre-CONTROL freshness/time checks, selective raw health, source closure
and actual normal/cut/failure/terminal consumers. Tests must preserve ordinary
P384 selection and P383 serialization/claim behavior, reject selector crossover,
isolate V1/V2 claims, and prohibit N/A replay after uncertainty. Current candidate
input comparison must prove `CHANGED_KEYS=[]`; existing matching ARM64 packages
can then be reused. The prior H0 review alone does not qualify these connections.

Before effects require fresh exact Process-v2 preparation, a usable verified A,
current physical recovery/attendance and a separately returned finite approval
binding this exact V2 graph. Success qualifies a bounded roundtrip only; visible
pixels, continuous liveness, kernel/PID1-stall recovery, unattended operation,
persistent storage and standing native-baseline authority remain unproved.
