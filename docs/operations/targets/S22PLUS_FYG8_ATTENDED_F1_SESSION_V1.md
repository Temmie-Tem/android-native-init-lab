# S22+ bounded attended F1 session V1

Status: **REVIEW_GATED_CAPABILITY**. No session is opened by this policy.
The capability is active only when the independent PASS_GO receipt at
`workspace/public/src/device-action/bindings/s22plus_attended_f1_session_v1_review.json`
has the exact current source identities, limits and activation identity enforced
by `s22plus_attended_f1_session_v1.reviewed`. Missing or stale review blocks it.

The exact target is SM-S906N/g0q/S906NKSS7FYG8 and the canonical FYG8 profile.
This policy specializes the common Bounded Attended F1 Sessions delegation.
The operator must remain physically present for the full device phase with the
existing demonstrated physical Download recovery route available. Flags record
an actual foreground operator statement; they cannot create consent/attendance.

A grant freezes at most three distinct reviewed READY manifests, their exact
candidate and common rollback artifacts, capability reviews and execution
closures, for at most two hours. Smaller explicit limits are allowed. The
agent cannot extend, replace or reopen a grant without new operator authority.
Qualification/build work occurs before the grant starts. Opening is H0 and uses
prior private target identity; fresh connected preparation and execute recheck
remain required for every run. No current healthy-device claim follows from H0.

The existing target lease and F1 owner serialize reservations/execution with D1
and ordinary F1. A durable reservation precedes per-run authority publication,
D0 execute recheck, journal approval attribution and Download intent. A partial
reservation blocks continuation; it cannot be retried or silently reclaimed.
Legacy exact-token execute rejects a session-reserved run. No generated token
stands in for the operator grant. The journal records grant digest, ordinal,
reservation digest and exact prepared binding without changing transfer states.

For this three-experiment batch, the preceding experiment must PASS and close
with validated final health before the next candidate. A proven pre-effect
abort closes its reservation without advancing the ordered catalog. A fresh
prepared run of that untransferred candidate may use another remaining
reservation; neither the aborted run nor its intent is reused. A NO_PROOF closes this dependent batch; it is not thereby
classified as a safety fault. This follows the selected batch success criterion,
not a new blanket safety gate on unrelated expected-negative research.

Unexplained failure, uncertain effect or recovery outside demonstrated scope
closes the grant to new experiments. Preserve pending state and the original
journal. Only already authorized recovery may continue with its exact binding;
otherwise park for physical intervention or separately reviewed recovery scope.
Expiry/withdrawal never cancel this rollback/final health. Recovery validation
reads historical session attribution without requiring an active grant or a
current research budget. Recovery from a nonterminal device journal first closes
the research grant, including hard process cuts that bypass exception cleanup;
a later CLOSED/PASS cannot erase that interruption. Already-CLOSED publication
repair and validated pre-effect ABORT reconciliation remain distinct. No candidate replay, automatic-recovery or unattended
claim follows from a successful session.

Activation requires independent review of the actual session module, common live
entry, raw/result/journal consumers, tests and common/target interactions. The
capability receipt binds that PASS_GO to exact source identities and limits.
Session execution specializes per-candidate human approval requirements only for
its admitted catalog, through the distinct `--execute-session` entry. Old
consumed manifests, prepared records, reviews and journals remain unchanged.
