# v347 Gate Refresh Generated Preflight Report

- date: `2026-05-19`
- scope: host-only V317 gate refresh coverage
- device command: none
- device mutation: none
- result: `PRE-COMMIT STRUCTURE PASS / POST-COMMIT REFRESH REQUIRED`

## Summary

v347 extends the V344 refresh helper so `--run-approved-preflight` also executes
the exact preflight command generated by the V340 handoff packet. This keeps the
operator-facing command under the same refresh evidence workflow.

## Code Change

- `scripts/revalidation/wifi_v317_gate_refresh.py`
  - imports `shlex`
  - adds `command_arg()`
  - adds `generated_handoff_preflight_step()`
  - appends `v340-generated-preflight` after `v342-approved-preflight` when
    `--run-approved-preflight` is set

## Pre-commit Validation

```bash
python3 -m py_compile scripts/revalidation/wifi_v317_gate_refresh.py
python3 scripts/revalidation/wifi_v317_gate_refresh.py \
  --run-approved-preflight \
  --out-dir tmp/wifi/v344-v317-gate-refresh \
  refresh || true
```

Observed pre-commit result:

```text
v340-generated-preflight step is present
device_commands_executed=false
device_mutations=false
overall refresh blocked as expected because the current source tree is dirty
```

## Post-commit Validation Plan

After commit, rerun V344 refresh on clean HEAD and expect both preflight steps to
PASS.

## Safety

- No live V317 proof was executed.
- No daemon start was performed.
- No Wi-Fi bring-up was performed.
