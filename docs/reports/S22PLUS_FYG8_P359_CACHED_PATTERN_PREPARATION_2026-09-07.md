# P359: cached RGB/grid/edge static-frame preparation

The operator authorized the next CACHED static-pattern preparation and D0/D1.
F1 remains separately approved after fresh preparation. P358 is consumed and
closed healthy; it is never replayable. No P359 candidate has transferred.

## P358 USB incident disposition

Independent H0 review joined the unchanged actual consumer with the immutable
P358 sequence-21 diagnostic. It records `usbfs-endpoint-departed` /
`UsbfsEndpointDeparture` before snapshot publication, after observation and
before rollback intent. All 33 original stat captures verified. The exact
endpoint and physical cause cannot be established by that diagnostic and remain
unproved. It is not proof of cable motion, a particular device fault or continuity.

The original fatal stop stayed intact. Same-journal recovery required a fresh
exact rollback endpoint and completed the sole rollback, CLOSED/19 and final
rooted FYG8 health. Independent review permits fresh successor preparation with
its own binding and finds no production repair necessary. No failed measurement
is accepted as a valid snapshot; no selector, topology, retry or recovery rule
is changed. Review receipt is `usb-incident-review.json`, SHA-256
`70c43f54a41a86c0d0d8b60e6d14a07673d857cd99fbeda27858c4245caad19d`.

## Selected bounded capability

P359 retains P358 CACHED ABGR8888, 1080x2340, pitch 4352, 30HS, all twelve
display additions, readiness, noise_layer_v1=0, color_fill=0, one buffer and one
blocking atomic request. Only painted pixels and identifying text change.
The upper-left area is red, upper-right blue and lower half green. A black
four-pixel grid starts every 120 pixels; an eight-pixel white border overrides
that grid. Padding stays white and all pixels are opaque.

Success requires an operator-observed regular grid, correct color positions and
complete border without breakup/displacement. Dispatch receipts cannot establish
renderer completion or screen output. No redraw, extra ioctl, kernel/module
patch or userspace retry is introduced. The inherited vendor DMA-map error
branch remains unqualified; CACHED success does not isolate cache coherency
from mmap/DMA-map timing. Physical Download and exact rollback/final health are
mandatory for every eventual F1 outcome.

## H0 construction and checks

Nine thin P359 sources reuse the sealed P353 machinery and sealed P358 renderer.
The shared registry adds the existing dispatch evidence class for P359. New
contract and ledger-family registration retain permanent boundaries.
All 77 byte-affecting source keys were listed and compared with Git changes
before construction. Source inputs were frozen; unrelated S20+ work and the
pre-existing P345 manifest are excluded.

The identity-only Image transformation retains kernel/module code. Fresh run ID:
`c359f1e0a90b5e6d7c8a9b0c1d2e3f0a`. Image:
`41490944B/0143a7a1f0ca848779267aff68bafe8c50edeecbfe3adef8a118b922e4ad215b`.
A/B init, renderer, boot and AP match. Candidate AP:
`30965801B/4e7c16222ed8bfcc36c3e59bf668ac339dff1178c73c5b324a2e5b73091a715d`.
Actual static qualification joins the sole boot member, ramdisk and artifact
identities. Display module bytes/plan, prerequisite/KMS identities and USB plan
match P358.

The static AArch64 executable was inspected with `file`; a byte-identical
executable fixture under QEMU produced all 10,183,680 bytes matching an
independent pixel oracle. Pixel output SHA-256:
`451038084fa8153d73375a3f38e702a885a67d961acbcde8de22ea6fcc0a4dc0`.
This is a host-memory check, not hardware scanout proof. Generated fake-DRM tests
exercise the CACHED/ABGR request, exact pixels, inherited-plane replacement and
negative/error paths without another commit. All 21 P359 tests passed after
actual static qualification; the initial missing-static-artifact test result is
preserved separately. Initial host identity planning attempts failed before
source publication; the corrected plan follows the consumer's ASCII marker and
same-length gzip contract and passes the actual image validator.

Private evidence: `workspace/private/outputs/s22plus_fyg8_p359/`.
Final independent review is PASS_GO, no open findings, receipt SHA-256
`8f29d0cc0f91fde270b09bae7323989374c0de3b995622f239ffd61098d699ae`.
All 107 execution-critical inputs and seven artifact identities reverify.
Offline promotion passes with common_offline_verified=true; ready manifest is
`4686B/8947edafd61231cb8b2ba6bac24d83128649c07026e8170d39e7ecdba1d8d174`.
P358 regression tests also pass 21/21, syntax checks pass for 14 Python files,
and repository boundary/diff checks pass. H0 was committed as `91acfd5ee9`.


## Connected baseline preparation

First D0 passed exact initial health, then preserved the expected baseline stop
in `p359-ready1-prepared-20260907-1`. Stop result:
`3252B/d37880f9c0b6f9bfdca48ea681c9e1895242273e2dd37d51d9015a7663d362af`.
The actual bundle and strict stop validator accepted that failure receipt;
it is non-reusable and no candidate or Download transition occurred.

The current D0/D1 preapproval authorized one ordinary reboot through the
unchanged reviewed P341/P320/P296 invocation. A fresh private metadata wrapper
binds the closed P358 result, current authority and the new stop. Self-test
passed before the sole execution. Result:
`2963B/d13e56c8f72cc88028152a38f929d2e213803120d49cf3415aad97e7d028448a`,
`PASS_P359_D1_EXACT_NORMAL_REBOOT_RETURN_HEALTH`. The returned boot ID differs;
exact rooted FYG8, original boot/supporting hashes and Android health passed.
Linked invocation and primitive receipts reverify. No retry or manual recovery
was needed; A90/S20+ received no command from this task.

Fresh D0 preparation in `p359-ready1-prepared-20260907-2` passed
`PASS_DEVICE_ACTION_D0_V2_CONNECTED_READ_ONLY`, result
`3261B/edb1ddc374c511b001e5ef3aa723d166def2e828f8a913bcda238f65a4504d6e`.
Prepared record is
`30321B/21201b11a058711bb9958faddaa88b75805b4a52d3b7683c39ba9a61e2055a9d`.
The fresh approval token is supplied directly to the operator and excluded from
tracked documents. No candidate transfer, Odin invocation, Download transition
or F1 authorization occurred. Actual `load_prepared` reopening passed.
The remaining F1 requires separately returned exact approval and the operator's
physical Download availability under the P359 target clause.
