# Native Init V2169 Transport Contract Live Validation

## Summary

- Date: `2026-06-08`
- Result: PASS
- Baseline promoted: `v2169-transport-contract`
- Init: `A90 Linux init 0.9.247 (v2169-transport-contract)`
- Boot image: `workspace/private/inputs/boot_images/boot_linux_v2169_transport_contract.img`
- Boot SHA256: `190b93d0741a6eeba17913c940f3bb398fed765f38532d5e0009840112166d6d`
- Source/build report: `docs/reports/NATIVE_INIT_V2169_TRANSPORT_CONTRACT_SOURCE_BUILD_2026-06-08.md`

## Boot Validation

- Flash/readback: PASS; local, remote, and boot-block prefix SHA256 all matched `190b93d0741a6eeba17913c940f3bb398fed765f38532d5e0009840112166d6d`.
- Boot verify: PASS; `A90 Linux init 0.9.247 (v2169-transport-contract)` observed.
- Selftest: PASS; `fail=0`, `warn=1`.
- Device contract: PASS; `status` emitted `transport.contract=1`, `transport.ncm=ready`, `transport.preferred=ncm`, `transport.reason=ncm-ready`.
- Host selector: PASS in the initial validation window; `selector_contract=1`, `transport_contract=1`, `selected=ncm`, `ncm_host=ready`, `serial_bridge=ready`.
- NCM smoke: PASS; 1MiB download `1.046s`, upload `0.508s`.

## Bridge Concurrency Validation

- Host bridge restart: PASS; live bridge restarted with the updated `serial_tcp_bridge.py`.
- Extra TCP client: PASS; live bridge returns `[bridge] busy: another client is active; retry later`.
- Host transaction lock: PASS; two concurrent `a90ctl status` commands completed with `rc=0`, `A90P1 END`, and `transport.contract=1`.
- Probe behavior: PASS; `a90_bridge doctor` reports `busy-serial-lock` as pass while another transaction holds the shared lock.
- Post-restart selftest: PASS; `selftest fail=0`.

## Current Follow-Up

- Host NCM can still become `present-no-link-local` after USB re-enumeration if the NetworkManager profile is pinned to an old `enx...` ifname.
- That host-side NCM readiness issue does not invalidate the boot/device contract. The selector safely falls back to `serial` when host link-local is absent.
- Follow-up owner: host NCM profile/readiness auto-recovery.

## Regeneration Policy

- This file is hand-written live evidence and should not be generated by the boot image builder.
- The source/build report may be regenerated by `build_native_init_boot_v2169_transport_contract.py`.
- Live validation evidence must stay in this separate report or a new live validation report to avoid accidental overwrite.
