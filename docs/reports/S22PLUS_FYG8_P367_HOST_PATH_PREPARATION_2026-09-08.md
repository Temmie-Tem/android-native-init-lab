# S22+ P367 host-path qualification and H0 preparation

P367 keeps P366's native functionality and changes run/namespace identity for
a fresh source/artifact binding. It connects the repaired Download-arrival
producer, resumed raw-capture allocation and target-scoped final health.
The initial unit below was H0 only, with no connected preparation or device
transfer. Subsequent authorized read-only preparation is recorded at the end.
P366 remains consumed; original historical evidence is preserved.

## Native scope and artifacts

The 83,736-byte native helper template equals P366 after normalization of
namespace/run identity. ARM64 open operands, module bytes/order/parameters,
renderer, bounded diagnostic protocol, ten swap submissions, one fixed Download
CONTROL and the original 30-second host return window remain unchanged.
Physical recovery and blocking-kernel-call limitations remain unchanged.
There is no new feature, unattended session or software-causality claim.

Fresh run ID: `c367f1e0a90b5e6d7c8a9b0c1d2e3f0b`. The checked namespace binds
all eleven P366 template files and its namespace by their original hashes.
Thin P367 wrappers expand those sealed definitions without editing them. A
pre-build H0 recursion from duplicate wrapper aliases was corrected before
qualification; no affected artifact or device run was consumed.

The Image uses the existing identity-only transformation from the exact P344
source: one fixed raw run-ID span plus same-length IKCONFIG recompression.
Image size, section layout and all undeclared spans are preserved and the
production artifact validator independently reproduces the transform.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Image | 41,490,944 | `a5695be0105b87132dead0c8207300d7cc2d50011b29246511ffaf68c6bd4657` |
| AP, A/B identical | 31,006,761 | `2d7c546e0a84d3bccaa3a67af6c9302a225a85a658c9d7646875fcb89a0013cb` |
| Static ARM64 init | 150,632 | `bd8401120e70ae704fc2f718d085c2ce08a3b9982ee7e3938e27595fda37bc76` |
| Unchanged renderer | 710,040 | `5e66fea1f312aa81853aa499336a35bc397886b7fbdafb7421b8db8d69d68919` |

The actual AP contains only the allowed regular `boot.img.lz4`. `file` confirms
both init and renderer are statically linked AArch64 executables. All 135
recorded build inputs match current bytes. The inherited build collector may
include additional imported host modules in other contexts; the build's exact
recorded inputs are retained rather than relabeled as a universal source count.
Static and execution closures separately bind their actual consumers.

Private output root: `workspace/private/outputs/s22plus_fyg8_p367/`.
Build result: 70,160 bytes, SHA-256
`5f3e2fcd35dbce981fb35d984cc890dfcfb281400ca1abf3b22b1ec0658e4dee`.
Candidate-static result: 66,741 bytes, SHA-256
`298b68ffa4118cc2f96852eda6629a55e3988f2eeac40c6298aa6d4320991750`.
These artifact reports are separate from the inherited 64-KiB prepared/live
state bound; no journal or unrelated record limit is widened.

## Joined H0 validation

Nine P367 tests include four representative real ARM64 syscall/provider checks
and a joined lifecycle using the actual generated P367 C console and codec,
actual P367 registry/capability selection, Samsung Download producer, Odin
receipt persistence/revalidation, ADB raw-capture client, temporary USB/Type-C
sysfs, scoped final-health producer and terminal result consumer. Kernel/device
behavior, transfer tool output and physical enumeration are fixtures. The
arrival and final-health receipts themselves are produced by their real paths;
no P318 rollback record or generic final-health result is fabricated.

The normal fixture reaches the P367 PASS terminal; a diagnostic writer failure
reaches NO_PROOF with verified fixture rollback. Both tolerate a foreign
Download endpoint introduced after completed rollback and report global absence
as false while target absence is true. Two live Download devices before the
candidate still stop before transfer. A client restart before EOF completes
from durable rollback without replaying candidate, CONTROL or rollback.

An injected cut after both EOF captures deliberately remains fail-closed on
the fixed existing output name. Its retained outputs remain unchanged and no
transfer is replayed. Resumed ordinal allocation is not a general cached-EOF
resume capability; this limitation is preserved, not counted as successful
terminal recovery. Reporting a completed rollback remains possible without
fabricating a canonical close, as in P366.

The combined final batch passes **216 tests**, including existing D0/F1,
Odin-core, P366, arrival, capture-resumption and scoped-final regressions.
All 15 new/touched Python files pass `py_compile`. Diff/content/link checks
pass. Independent source/artifact review returned `PASS_GO`; its receipt
SHA-256 is `96ce9299fa310c572932e3bb7709d8689885ab5e87b7cd451ae8a3d6a7698a3f`.
The actual bundle rehearsal returned
`PASS_P367_PROCESS_V2_PROMOTION_REHEARSAL_HOST_ONLY`, with device contact,
Odin invocation and run-directory creation all false.

## Next boundary

H0 readiness is capability/artifact evidence only. Connected preparation needs
current exact target/health binding and existing session conditions; attended
F1 still requires the separately returned fresh approval. P367 has no consumed
candidate, CONTROL or rollback. A90 and S20+ received no command or source edit
from this unit.


## H0 ready publication

The publishing entry returned `PASS_P367_PROCESS_V2_READY_MANIFEST_HOST_ONLY` and reopened the actual
ready manifest with the common bundle verifier. Manifest
[`s22plus_fyg8_p367_process_v2_ready_1.json`](../../workspace/public/src/device-action/manifests/s22plus_fyg8_p367_process_v2_ready_1.json)
is 5054 bytes, SHA-256
`6cdbf9863d9763671975dad49f086f6c6361ebed464173b704f8f29106790689`. Bundle SHA-256:
`74bf9b4af2bb51dd595abfd82e17d45199adf9108bfb5e237692a1098cf60446`. No connected run directory was created;
device contact, Odin invocation and live authorization remain false.


## Subsequent connected preparation

At the operator's request, the actual ready1 manifest completed the existing
bounded connected D0 profile and fresh preparation, without a D1 reboot or F1
transfer. Exact target, rooted FYG8 health, original boot/supporting hashes and
global Download absence passed. A separate real prepared reopen also passed.
The private preparation is 33,105 bytes, SHA-256
`b3d524d2eb144d27abd726e860f31fa23c157bc6e74c6accdd1b5b53a201e067`,
in run `p367-ready1-prepared-20260908-1`. The fresh approval code remains private
and requires explicit operator return with physical attendance before F1.

The same-boot D0 evidence also enabled the independently reviewed H0 closure
of P366, now CLOSED/19 with NO_PROOF and verified final health; see its
[closure record](S22PLUS_FYG8_P366_NATIVE_USB_DEPARTURE_PREPARATION_2026-09-08.md#subsequent-h0-closure-from-fresh-p367-d0).
No P367 candidate, CONTROL or rollback has been consumed. A90 and S20+ received
no command from this preparation; their unrelated source work is outside it.


## Approved invocation: pre-candidate observer-arm abort

The operator returned the exact fresh approval. The normal production execute
entry revalidated the unchanged source closure and fresh connected target/health,
then created the journal and recorded approval. The host ModemManager guard did
not emit its arm frame within the existing 30-second bound. Its capture records
30,114 ms, zero output bytes and returncode=null while the child was still alive;
the writer's `timed_out=false` describes capture publication, not successful guard
arming. The original guard owner subsequently released the failed child.

The host process was observed waiting in pkexec, and the bounded polkit log
records failed authentication at `2026-09-07T23:35:59Z`. No authentication
completion was observed before the guard deadline; password error, dismissal or
lack of response is not separately established. No timeout or privilege rule
was changed. The temporary runtime udev guard rule is absent after termination.

Canonical result: `FAIL_F1_V2_PRE_CANDIDATE_DOWNLOAD`, outcome
`candidate_observer_arm_failed_before_candidate`, `ABORTED/4`,
recovery_required=false. The journal abort timestamp is
`2026-09-07T23:35:58.891950Z`. Result size is 1,351 bytes, SHA-256
`c106c66438d57f1a3b6481d1f71bba7abd955cf82f6c7d999c9985e8ce0e5ba2`.
No candidate Download request intent was created; candidate, CONTROL and
rollback counts remain zero. No partition transfer, native observation or
rollback health is claimed from this invocation. The successful execute-time
Android health recheck precedes the host-only failure.

Raw guard capture is retained in the exact prepared run; bounded execution and
polkit logs are under the private P367 output root. The returned approval is
stopped and was not replayed. A later attempt requires new preparation and
fresh approval under Process v2, with the existing host authentication ready.
This failed host start does not consume the candidate artifact. A90 and S20+
received no command from this invocation.
