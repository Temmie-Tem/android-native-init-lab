# Public Source Workspace

This tree holds current tracked source entrypoints that should survive a fresh
clone without private payloads. Legacy historical sources may remain in their
original repository paths while active entrypoints migrate here.

Private inputs and generated outputs stay under `../private/`.
