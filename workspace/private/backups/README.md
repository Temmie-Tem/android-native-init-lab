# Private Backups

Local device/partition/boot backups live here after workspace migration. Payloads
are ignored by Git. Publish only redacted SHA/checklist metadata under
`workspace/public/manifests/` or `docs/artifacts/`.
