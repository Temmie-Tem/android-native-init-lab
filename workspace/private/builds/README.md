# Private Build Outputs

Generated build outputs live here. Git ignores payloads in this tree; only the
directory scaffold README files are tracked.

Use this tree for ramdisk directories, cpio files, compiled init/helper binaries,
intermediate manifests, and local-only test boot artifacts.
