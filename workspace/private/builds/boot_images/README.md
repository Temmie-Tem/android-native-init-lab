# Boot Image Build Outputs

Use this directory for private boot image experiments that are not yet promoted
to trusted inputs. Baseline images used as rollback or next-build inputs belong
under `../../inputs/boot_images/`.
